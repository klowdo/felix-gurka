#!/usr/bin/env bash
# direnv configuration for Felix Gurka development environment

# Use the Nix flake for development shell
use flake

# Optional: Add any additional environment variables or setup here
# export CUCUMBER_WORLD_DEBUG=true
# export NODE_ENV=development

# Show helpful message when entering the directory
echo "🥒 Entering Felix Gurka development environment..."
echo "Available commands: npm run dev, serve, live-server, http-server"